services:
  postgres:
    image: 'postgres:latest'
    container_name: 'shipping.service.postgres'
    environment:
      - 'POSTGRES_DB=mydatabase'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '${POSTGRES_PORT}:5432'
