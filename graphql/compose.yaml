services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=graphql_db'
      - 'POSTGRES_USER=graphql_user'
      - 'POSTGRES_PASSWORD=secret'
    ports:
      - '15432:5432'
